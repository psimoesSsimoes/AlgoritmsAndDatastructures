

public class LegoSolver {

      int[] an_array = new int[colums];

      public LegoSolver(int colums){

      }
      public void processLine(String a_line){
          char ch = a_line.charAt(0);
          for (int i = 1 ; i < a_line.length ; i++){
            if(ch=='.')
              an_array[0]++;
            if(mosaics[k].charAt(i) == ch)
              counter++;
          }
      }
      public int getNrCombinations(){
        return
      }
